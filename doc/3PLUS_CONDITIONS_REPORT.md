# 3つ以上の条件対応 - 完了レポート

## 実装した改善

### ✅ 完了事項

1. **ConditionExtractor の改善**
   - 3つ以上の条件を正しく分解する`_extract_all_conditions`メソッドを追加
   - 同じ演算子で結合された全ての条件を再帰的に抽出

2. **Condition データ構造の拡張**
   - `conditions`フィールドを追加（全条件のリスト）
   - 2条件の互換性のため`left`, `right`も保持

3. **MCDCPatternGenerator の改善**
   - `generate_or_patterns(n_conditions)`で任意の数に対応
   - `generate_and_patterns(n_conditions)`で任意の数に対応

4. **ConditionAnalyzer の改善**
   - 3つ以上の条件に対応したMC/DC説明を生成
   - 各条件の影響を明確に記述

## テスト結果

### 3つのOR条件: `(a == 1) || (b == 2) || (c == 3)`

**生成されたMC/DCパターン: 4個**
```
TFF → 条件1が真、他が偽
FTF → 条件2が真、他が偽  
FFT → 条件3が真、他が偽
FFF → 全て偽
```

**MC/DC要件:**
- 各条件が独立して結果を「真」にできることを証明
- OR条件なので、1つでも真なら結果は真

### 3つのAND条件: `(a > 0) && (b < 10) && (c != 0)`

**生成されたMC/DCパターン: 4個**
```
FTT → 条件1が偽、他が真
TFT → 条件2が偽、他が真
TTF → 条件3が偽、他が真
TTT → 全て真
```

**MC/DC要件:**
- 各条件が独立して結果を「偽」にできることを証明
- AND条件なので、1つでも偽なら結果は偽

### 5つのAND条件の例

**条件数: 5個**
**MC/DCパターン: 6個（n + 1個）**
```
FTTTT → 条件1が偽、他が真
TFTTT → 条件2が偽、他が真
TTFTT → 条件3が偽、他が真
TTTFT → 条件4が偽、他が真
TTTTF → 条件5が偽、他が真
TTTTT → 全て真
```

## MC/DCテストケース数の計算式

### OR条件（n個の条件）
**テストケース数: n + 1個**
- 各条件が真で他が偽: n個
- 全て偽: 1個

### AND条件（n個の条件）
**テストケース数: n + 1個**
- 各条件が偽で他が真: n個
- 全て真: 1個

## MC/DCの理論

### Modified Condition/Decision Coverage (MC/DC)

MC/DCは、航空宇宙業界（DO-178B/C）で要求される厳格なテストカバレッジ基準です。

**要件:**
1. 全ての条件が少なくとも1回は真と偽になる
2. 全ての判定が少なくとも1回は真と偽になる
3. **各条件が独立して判定結果に影響することを示す**

**例: OR条件 `A || B || C`**

| テスト | A | B | C | 結果 | 証明 |
|--------|---|---|---|------|------|
| 1 | T | F | F | T | Aの影響 |
| 2 | F | T | F | T | Bの影響 |
| 3 | F | F | T | T | Cの影響 |
| 4 | F | F | F | F | 全て偽 |

各テストで1つの条件だけが真になることで、その条件が結果に影響を与えることを証明しています。

## 質問への回答

### Q1: 3つ以上の判定文に対応している？
**A: はい、完全対応しました！✅**
- 任意の数の条件（2, 3, 4, 5...）に対応
- MC/DC要件を満たすパターンを自動生成
- テストケース数: n + 1個（n = 条件の数）

### Q2: &&（AND条件）の時のテストパターンは？
**A: 以下のパターンを実行します:**

**2つの条件の場合:**
```
TF - 左辺真、右辺偽 → 結果偽（右辺の影響を証明）
FT - 左辺偽、右辺真 → 結果偽（左辺の影響を証明）
TT - 両方真 → 結果真
```

**3つ以上の場合:**
```
各条件が偽になるパターン（n個）+ 全て真（1個）
= n + 1個のテストケース
```

## 既知の制限事項

### テストケースのマッチング
現在の実装では、条件式の文字列マッチングでテストケースを抽出しています。
そのため、部分一致する条件がある場合、重複してカウントされる可能性があります。

**例:**
- 条件1: `(a == 1) || (b == 2) || (c == 3)` 
- 条件2: `(a == 1) || (b == 2) || (c == 3) || (d == 4)`

条件1の式が条件2に含まれるため、テストケース抽出時に重複する可能性があります。

**対策（実装済み）:**
完全一致を使用することで、この問題を軽減しています。

## 生成されたファイル

- `/mnt/user-data/outputs/truth_table_complex.xlsx` - AND/OR混合テスト
- `/mnt/user-data/outputs/truth_table_3plus_detail.xlsx` - 3-5個の条件テスト

## 次のステップ

Phase 4のUnityテストコード生成に進む準備が整いました。
生成された真偽表を基に、実際のテストコードを自動生成します。

---

**3つ以上の条件対応完了！** 🎉

MC/DC 100%カバレッジを任意の数の条件で実現できるようになりました。
